# Calender_GUI_Java
Calendar GUI application developed in Java (Swing & AWT) that allows users to add, view, save, and remove events on specific dates. Events are stored permanently using a text file so they remain available even after restarting the application.
